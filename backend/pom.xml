<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.7</version>
        <relativePath/>
    </parent>

    <groupId>br.com.leoferolive</groupId>
    <artifactId>nossalista</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>NossaLista</name>
    <description>NossaLista - Shared Lists Application</description>

    <properties>
        <java.version>25</java.version>
        <jjwt.version>0.12.3</jjwt.version>
        <jacoco.version>0.8.14</jacoco.version>
        <maven.checkstyle.version>3.6.0</maven.checkstyle.version>
        <maven.pmd.version>3.28.0</maven.pmd.version>
        <spotbugs.maven.plugin.version>4.9.8.1</spotbugs.maven.plugin.version>
        <springdoc.version>3.0.2</springdoc.version>
        <!-- swagger-ui 5.32.6 bundles DOMPurify 3.4.0 (>= 3.2.7), corrige CVE-2026-41238/41239/41240 -->
        <swagger.ui.version>5.32.6</swagger.ui.version>
        <!-- Override de versões para CVEs além do que o BOM do Spring Boot gerencia -->
        <postgresql.version>42.7.11</postgresql.version>
        <!-- 11.0.23 corrige CVE-2026-55276/53434 (9.1) e CVE-2026-53404 (7.3), presentes em 11.0.22 -->
        <tomcat.version>11.0.23</tomcat.version>
        <netty.version>4.2.15.Final</netty.version>
        <checkstyle.config.file>checkstyle/checkstyle.xml</checkstyle.config.file>
        <checkstyle.failsOnError>true</checkstyle.failsOnError>
        <testGroups/>
        <!-- Spring AI 2.0.0 GA (2026-06-12) é o primeiro trem estável desenhado para
             Spring Boot 4.0/4.1 — ver D-020 em docs/DECISIONS.md. -->
        <spring-ai.version>2.0.0</spring-ai.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.webjars</groupId>
                <artifactId>swagger-ui</artifactId>
                <version>${swagger.ui.version}</version>
            </dependency>
            <!-- Override transitive jose4j from web-push to fix CVE-2023-31582 and CVE-2024-29371 -->
            <dependency>
                <groupId>org.bitbucket.b_c</groupId>
                <artifactId>jose4j</artifactId>
                <version>0.9.6</version>
            </dependency>
            <!-- Override transitive async-http-client de web-push:5.1.1 (vinha em 2.10.4)
                 para 2.15.0 e corrigir CVE-2026-45300 (vazamento de Cookie em redirect
                 cross-origin). Mesmo major 2.x, compatível. O async-http-client-netty-utils
                 acompanha como dependência interna do próprio async-http-client. -->
            <dependency>
                <groupId>org.asynchttpclient</groupId>
                <artifactId>async-http-client</artifactId>
                <version>2.15.0</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Spring Boot Starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-client</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- MCP server (Streamable HTTP) — ver docs/DECISIONS.md D-020.
             Exclui io.micrometer:context-propagation: sua mera presença no
             classpath faz o Spring Security registrar um ThreadLocalAccessor
             de SecurityContext no ContextRegistry global do Micrometer, que
             interfere na captura/restauração de thread-locals ao redor de
             Observations HTTP (actuator) — quebrando testes que populam
             SecurityContextHolder manualmente antes do MockMvc.perform (ver
             docs/quality-gate-debt.md). O SDK MCP usa reactor-core apenas
             internamente (client/server síncronos bloqueiam no Mono), então
             reactor-core continua necessário e é mantido. -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-mcp-server-webmvc</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>io.micrometer</groupId>
                    <artifactId>context-propagation</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Micrometer Prometheus registry — expõe /actuator/prometheus -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!-- Database -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Flyway -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>

        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Email -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>

        <!-- WebSocket -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>

        <!-- Web Push (VAPID) -->
        <dependency>
            <groupId>nl.martijndwars</groupId>
            <artifactId>web-push</artifactId>
            <version>5.1.1</version>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcprov-jdk18on</artifactId>
            <version>1.80</version>
        </dependency>

        <!-- SpringDoc OpenAPI (Swagger) -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-test-autoconfigure</artifactId>
            <version>4.0.3</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>1.4.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>${maven.checkstyle.version}</version>
                <configuration>
                    <configLocation>${checkstyle.config.file}</configLocation>
                    <suppressionsLocation>checkstyle/suppressions.xml</suppressionsLocation>
                    <includeTestSourceDirectory>false</includeTestSourceDirectory>
                    <consoleOutput>true</consoleOutput>
                    <failsOnError>${checkstyle.failsOnError}</failsOnError>
                </configuration>
                <executions>
                    <execution>
                        <id>checkstyle-validation</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
                <version>${maven.pmd.version}</version>
                <configuration>
                    <targetJdk>${java.version}</targetJdk>
                    <printFailingErrors>true</printFailingErrors>
                    <failOnViolation>true</failOnViolation>
                    <format>xml</format>
                    <rulesets>
                        <ruleset>pmd/ruleset.xml</ruleset>
                    </rulesets>
                    <!--
                        CPD (duplicação): minimumTokens agressivo (default 100).
                        IA copia-cola lógica em vez de reusar. Começa em 80; medir e
                        descer. ignoreIdentifiers/Literals=false torna a detecção mais
                        estrita.
                    -->
                    <minimumTokens>80</minimumTokens>
                    <ignoreIdentifiers>false</ignoreIdentifiers>
                    <ignoreLiterals>false</ignoreLiterals>
                </configuration>
                <executions>
                    <execution>
                        <id>pmd-check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                    <!--
                        WARNING inicial: goal `cpd` (report-only) gera target/cpd.xml
                        sem quebrar o build. Promover a `cpd-check` (blocker) após medir
                        o baseline e excluir DTOs/mappers que inflam falso-positivo.
                    -->
                    <execution>
                        <id>cpd-report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>cpd</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>${spotbugs.maven.plugin.version}</version>
                <configuration>
                    <effort>Max</effort>
                    <threshold>High</threshold>
                    <failOnError>true</failOnError>
                    <includeTests>false</includeTests>
                </configuration>
                <executions>
                    <!-- Gate bloqueante existente: SpotBugs puro, High (inalterado). -->
                    <execution>
                        <id>spotbugs-check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                    <!--
                        FindSecBugs (SAST): varredura de segurança em threshold Medium.
                        WARNING inicial — usa o goal `spotbugs` (report-only, NUNCA falha
                        o build), não o `check`. Importante: o goal `check` forka o
                        `spotbugs` e o fork IGNORA config de nível de execução (o
                        findsecbugs e o nome custom do XML não seriam aplicados). O goal
                        `spotbugs` roda a análise direto e honra esta config. Gera
                        spotbugs-security.xml para decoração de PR. Promover a blocker
                        (goal `check` + findsecbugs em nível de plugin) após triagem.
                    -->
                    <execution>
                        <id>spotbugs-security</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>spotbugs</goal>
                        </goals>
                        <configuration>
                            <effort>Max</effort>
                            <threshold>Medium</threshold>
                            <includeTests>false</includeTests>
                            <xmlOutput>true</xmlOutput>
                            <spotbugsXmlOutputFilename>spotbugs-security.xml</spotbugsXmlOutputFilename>
                            <plugins>
                                <plugin>
                                    <groupId>com.h3xstream.findsecbugs</groupId>
                                    <artifactId>findsecbugs-plugin</artifactId>
                                    <version>1.14.0</version>
                                </plugin>
                            </plugins>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!--
                O banco NVD é aquecido no main (nvd-cache-warmer.yml) e as PRs rodam
                com -DautoUpdate=false contra o cache. O dependency-check 12.x grava o
                banco H2 em data/<versão>/odc.mv.db (subdiretório versionado): o guard
                de cache do ci.yml procura o *.mv.db de forma recursiva por causa disso.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <groups>${testGroups}</groups>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <configuration>
                    <excludes>
                        <exclude>br/com/leoferolive/nossalista/websocket/**</exclude>
                        <exclude>br/com/leoferolive/nossalista/listitem/dto/ListItemMapper*</exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>jacoco-report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>jacoco-check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.80</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.75</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!--
                PIT (mutation testing). ADIADO: confirmado INCOMPATÍVEL com Java 25
                em 2026-06-03 ("Unsupported class file major version 69" — PIT 1.19.1
                não lê bytecode JDK 25 via ASM; ver hcoles/pitest#1439). O step de CI
                está desativado (if: false). Plugin mantido aqui (não roda no `verify`
                — sem <executions>) para reativar quando o PIT suportar JDK 25:
                bumpar a versão e religar o step. Invocação prevista:
                `org.pitest:pitest-maven:scmMutationCoverage` só nas classes alteradas.
                Excludes espelham o JaCoCo.
            -->
            <plugin>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-maven</artifactId>
                <version>1.19.1</version>
                <dependencies>
                    <dependency>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-junit5-plugin</artifactId>
                        <version>1.2.3</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <targetClasses>
                        <param>br.com.leoferolive.nossalista.*</param>
                    </targetClasses>
                    <excludedClasses>
                        <param>br.com.leoferolive.nossalista.websocket.*</param>
                        <param>br.com.leoferolive.nossalista.*.dto.*Mapper*</param>
                    </excludedClasses>
                    <outputFormats>
                        <param>XML</param>
                        <param>HTML</param>
                    </outputFormats>
                    <timestampedReports>false</timestampedReports>
                    <historyInputFile>${project.basedir}/target/pit-history/history.bin</historyInputFile>
                    <historyOutputFile>${project.basedir}/target/pit-history/history.bin</historyOutputFile>
                    <mutationThreshold>70</mutationThreshold>
                    <coverageThreshold>0</coverageThreshold>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>regression-tests</id>
            <properties>
                <testGroups>regression</testGroups>
            </properties>
        </profile>
        <profile>
            <id>strict-quality</id>
            <properties>
                <checkstyle.config.file>checkstyle/checkstyle-strict.xml</checkstyle.config.file>
            </properties>
        </profile>
    </profiles>

</project>
