# TEMPORARIAMENTE DESATIVADO: workflow quebrado.
# Reativar quando o pipeline externo estiver corrigido.
#
# name: Deploy to Homelab
#
# on:
#   push:
#     branches: [main]
#
# jobs:
#   deploy:
#     uses: leoferolive/self-workflows/.github/workflows/deploy.yml@main
#     with:
#       app_name: nossalista
#       app_namespace: nossalista
#       app_type: backend
#       dockerfile_path: ./Dockerfile
#       java_version: '21'
#       k8s_path: ./k8s
#     secrets: inherit
